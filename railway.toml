[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn app:server --bind 0.0.0.0:$PORT --workers 2 --timeout 120 --keep-alive 5 --log-level info"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[environments.production]
PORT = "8050"
